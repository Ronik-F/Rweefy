import WorkContent from "../WorkContent";

export const metadata = {
  title: "Selected Work — Ronik Koirala",
  description: "Flagship case studies and production applications.",
};

export default function SelectedWorkPage() {
  return <WorkContent initialCategory="selected" />;
}
